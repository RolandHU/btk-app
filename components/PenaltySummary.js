export default function PenaltySummary({ penalties, data, func }) {
  console.log(penalties)

  return (
    <main className="absolute md:relative w-full md:w-2/5 md:max-w-2xl h-screen border">
      <p>a</p>
    </main>
  )
}